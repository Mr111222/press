import CbAside from 'element-ui/packages/aside/index.js'

const install = CbAside.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbAside', CbAside)
}

export default CbAside
