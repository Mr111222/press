import CbOptionGroup from 'element-ui/packages/option-group/index.js'

const install = CbOptionGroup.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbOptionGroup', CbOptionGroup)
}

export default CbOptionGroup
