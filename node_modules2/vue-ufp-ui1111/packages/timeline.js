import CbTimeline from 'element-ui/packages/timeline/index.js'

const install = CbTimeline.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbTimeline', CbTimeline)
}

export default CbTimeline
