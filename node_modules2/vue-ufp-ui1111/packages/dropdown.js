import CbDropdown from 'element-ui/packages/dropdown/index.js'

const install = CbDropdown.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbDropdown', CbDropdown)
}

export default CbDropdown
