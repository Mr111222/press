import CbForm from 'element-ui/packages/form/index.js'

const install = CbForm.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbForm', CbForm)
}

export default CbForm
