import CbBreadcrumb from 'element-ui/packages/breadcrumb/index.js'

const install = CbBreadcrumb.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbBreadcrumb', CbBreadcrumb)
}

export default CbBreadcrumb
