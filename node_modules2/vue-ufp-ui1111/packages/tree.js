import CbTree from 'element-ui/packages/tree/index.js'

const install = CbTree.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbTree', CbTree)
}

export default CbTree
