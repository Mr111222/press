import CbRadio from 'element-ui/packages/radio/index.js'

const install = CbRadio.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbRadio', CbRadio)
}

export default CbRadio
