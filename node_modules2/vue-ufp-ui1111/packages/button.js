import CbButton from 'element-ui/packages/button/index.js'

const install = CbButton.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbButton', CbButton)
}

export default CbButton
