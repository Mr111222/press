import CbSteps from 'element-ui/packages/steps/index.js'

const install = CbSteps.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbSteps', CbSteps)
}

export default CbSteps
