import CbMenu from 'element-ui/packages/menu/index.js'

const install = CbMenu.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbMenu', CbMenu)
}

export default CbMenu
