import CbCheckboxGroup from 'element-ui/packages/checkbox-group/index.js'

const install = CbCheckboxGroup.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbCheckboxGroup', CbCheckboxGroup)
}

export default CbCheckboxGroup
