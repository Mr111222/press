import CbDialog from 'element-ui/packages/dialog/index.js'

const install = CbDialog.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbDialog', CbDialog)
}

export default CbDialog
