import CbSelect from 'element-ui/packages/select/index.js'

const install = CbSelect.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbSelect', CbSelect)
}

export default CbSelect
