import CbSubmenu from 'element-ui/packages/submenu/index.js'

const install = CbSubmenu.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbSubmenu', CbSubmenu)
}

export default CbSubmenu
