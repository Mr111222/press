import Sticky from './sticky.vue'
const install = Sticky.install = function (Vue, options = {}) {
  if (install.installed) return
  if (!options.scoped) {
    Vue.component(options.name || Sticky.name, Sticky)
  }
}

export default Sticky
