import CbTimePicker from 'element-ui/packages/time-picker/index.js'

const install = CbTimePicker.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbTimePicker', CbTimePicker)
}

export default CbTimePicker
