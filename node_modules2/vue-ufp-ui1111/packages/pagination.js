import CbPagination from 'element-ui/packages/pagination/index.js'

const install = CbPagination.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbPagination', CbPagination)
}

export default CbPagination
