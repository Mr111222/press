import CbTag from 'element-ui/packages/tag/index.js'

const install = CbTag.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbTag', CbTag)
}

export default CbTag
