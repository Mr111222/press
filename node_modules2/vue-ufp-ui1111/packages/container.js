import CbContainer from 'element-ui/packages/container/index.js'

const install = CbContainer.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbContainer', CbContainer)
}

export default CbContainer
