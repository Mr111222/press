/*eslint-disable */
import NoticeBar from './NoticeBar.vue'
let install = NoticeBar.install = function (Vue, options = {}) {
  if (install.installed) return
  if (!options.scoped) {
    Vue.component(options.name || NoticeBar.name, NoticeBar)
  }
}

export default NoticeBar
