import CbCheckboxButton from 'element-ui/packages/checkbox-button/index.js'

const install = CbCheckboxButton.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbCheckboxButton', CbCheckboxButton)
}

export default CbCheckboxButton
