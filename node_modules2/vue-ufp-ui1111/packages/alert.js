import CbAlert from 'element-ui/packages/alert/index.js'

const install = CbAlert.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbAlert', CbAlert)
}

export default CbAlert
