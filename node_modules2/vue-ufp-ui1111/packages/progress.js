import CbProgress from 'element-ui/packages/progress/index.js'

const install = CbProgress.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbProgress', CbProgress)
}

export default CbProgress
