import CbScrollbar from 'element-ui/packages/scrollbar/index.js'

const install = CbScrollbar.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbScrollbar', CbScrollbar)
}

export default CbScrollbar
