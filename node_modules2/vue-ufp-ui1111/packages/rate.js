import CbRate from 'element-ui/packages/rate/index.js'

const install = CbRate.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbRate', CbRate)
}

export default CbRate
