import CbCheckbox from 'element-ui/packages/checkbox/index.js'

const install = CbCheckbox.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbCheckbox', CbCheckbox)
}

export default CbCheckbox
