import CbFooter from 'element-ui/packages/footer/index.js'

const install = CbFooter.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbFooter', CbFooter)
}

export default CbFooter
