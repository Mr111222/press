import CbHeader from 'element-ui/packages/header/index.js'

const install = CbHeader.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbHeader', CbHeader)
}

export default CbHeader
