import CbIcon from 'element-ui/packages/icon/index.js'

const install = CbIcon.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbIcon', CbIcon)
}

export default CbIcon
