import CbPopover from 'element-ui/packages/popover/index.js'

const install = CbPopover.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbPopover', CbPopover)
}

export default CbPopover
