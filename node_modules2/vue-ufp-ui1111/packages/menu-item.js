import CbMenuItem from 'element-ui/packages/menu-item/index.js'

const install = CbMenuItem.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbMenuItem', CbMenuItem)
}

export default CbMenuItem
