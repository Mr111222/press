import CbTransfer from 'element-ui/packages/transfer/index.js'

const install = CbTransfer.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbTransfer', CbTransfer)
}

export default CbTransfer
