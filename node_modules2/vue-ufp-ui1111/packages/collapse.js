import CbCollapse from 'element-ui/packages/collapse/index.js'

const install = CbCollapse.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbCollapse', CbCollapse)
}

export default CbCollapse
