import CbTimelineItem from 'element-ui/packages/timeline-item/index.js'

const install = CbTimelineItem.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbTimelineItem', CbTimelineItem)
}

export default CbTimelineItem
