import CbFormItem from 'element-ui/packages/form-item/index.js'

const install = CbFormItem.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbFormItem', CbFormItem)
}

export default CbFormItem
