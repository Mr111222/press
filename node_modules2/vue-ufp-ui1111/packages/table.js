import CbTable from 'element-ui/packages/table/index.js'

const install = CbTable.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbTable', CbTable)
}

export default CbTable
