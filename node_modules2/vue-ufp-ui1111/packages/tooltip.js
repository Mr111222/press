import CbTooltip from 'element-ui/packages/tooltip/index.js'

const install = CbTooltip.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbTooltip', CbTooltip)
}

export default CbTooltip
