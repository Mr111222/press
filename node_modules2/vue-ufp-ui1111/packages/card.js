import CbCard from 'element-ui/packages/card/index.js'

const install = CbCard.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbCard', CbCard)
}

export default CbCard
