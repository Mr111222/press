import CbCollapseItem from 'element-ui/packages/collapse-item/index.js'

const install = CbCollapseItem.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbCollapseItem', CbCollapseItem)
}

export default CbCollapseItem
