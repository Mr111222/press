import CbTableColumn from 'element-ui/packages/table-column/index.js'

const install = CbTableColumn.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbTableColumn', CbTableColumn)
}

export default CbTableColumn
