import CbSlider from 'element-ui/packages/slider/index.js'

const install = CbSlider.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbSlider', CbSlider)
}

export default CbSlider
