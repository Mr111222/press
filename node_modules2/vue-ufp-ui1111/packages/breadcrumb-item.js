import CbBreadcrumbItem from 'element-ui/packages/breadcrumb-item/index.js'

const install = CbBreadcrumbItem.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbBreadcrumbItem', CbBreadcrumbItem)
}

export default CbBreadcrumbItem
