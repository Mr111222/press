import CbCascader from 'element-ui/packages/cascader/index.js'

const install = CbCascader.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbCascader', CbCascader)
}

export default CbCascader
