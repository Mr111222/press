/*eslint-disable */
const ECharts = require('./components/ECharts.vue').default 
const install = ECharts.install = function (Vue, options = {}) {
  if (install.installed) return
  if (!options.scoped) {
    Vue.component(options.name || ECharts.name, ECharts)
  }
}
export default ECharts