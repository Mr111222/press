const path = require('path')
module.exports = {
  externals: [
    'resize-detector',
    'lodash/debounce',
    /echarts\/.*$/
  ],
  globals: {
    '@babel/runtime/helpers/interopRequireDefault': 'interopRequireDefault',
    'resize-detector': 'resize-detector',
    '@babel-runtime/helpers/toConsumableArray': 'toConsumableArray',    
    'lodash/debounce': 'debounce',
    'echarts/lib/echarts': 'echarts',
    'echarts/echarts.all': 'echarts/echarts.all'
  },
  module: {
    rules: [
      {
        test: path.join(__dirname, '../src/main.js'),
        loader: [path.join(__dirname, '../src/main-loader.js')]
      }
    ]
  }
}
