import CbTabs from 'element-ui/packages/tabs/index.js'

const install = CbTabs.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbTabs', CbTabs)
}

export default CbTabs
