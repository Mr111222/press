import CbTabPane from 'element-ui/packages/tab-pane/index.js'

const install = CbTabPane.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbTabPane', CbTabPane)
}

export default CbTabPane
