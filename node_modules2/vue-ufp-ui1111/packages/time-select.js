import CbTimeSelect from 'element-ui/packages/time-select/index.js'

const install = CbTimeSelect.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbTimeSelect', CbTimeSelect)
}

export default CbTimeSelect
