import CbInput from 'element-ui/packages/input/index.js'

const install = CbInput.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbInput', CbInput)
}

export default CbInput
