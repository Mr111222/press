import cbMobiSelect from './components/mselect.vue'

let install = cbMobiSelect.install = function (Vue, options = {}) {
  if (install.installed) return
  if (!options.scoped) {
    Vue.component(options.name || cbMobiSelect.name, cbMobiSelect)
  }
}

export default cbMobiSelect
