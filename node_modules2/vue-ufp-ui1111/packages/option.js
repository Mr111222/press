import CbOption from 'element-ui/packages/option/index.js'

const install = CbOption.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbOption', CbOption)
}

export default CbOption
