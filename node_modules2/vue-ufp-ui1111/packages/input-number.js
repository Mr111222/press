import CbInputNumber from 'element-ui/packages/input-number/index.js'

const install = CbInputNumber.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbInputNumber', CbInputNumber)
}

export default CbInputNumber
