import CbColorPicker from 'element-ui/packages/color-picker/index.js'

const install = CbColorPicker.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbColorPicker', CbColorPicker)
}

export default CbColorPicker
