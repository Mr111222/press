import CbRow from 'element-ui/packages/row/index.js'

const install = CbRow.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbRow', CbRow)
}

export default CbRow
