import CbSpinner from 'element-ui/packages/spinner/index.js'

const install = CbSpinner.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbSpinner', CbSpinner)
}

export default CbSpinner
