import CbCol from 'element-ui/packages/col/index.js'

const install = CbCol.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbCol', CbCol)
}

export default CbCol
