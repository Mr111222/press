import CbSwitch from 'element-ui/packages/switch/index.js'

const install = CbSwitch.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbSwitch', CbSwitch)
}

export default CbSwitch
