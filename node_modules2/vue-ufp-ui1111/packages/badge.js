import CbBadge from 'element-ui/packages/badge/index.js'

const install = CbBadge.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbBadge', CbBadge)
}

export default CbBadge
