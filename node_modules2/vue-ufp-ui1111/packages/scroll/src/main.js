import Scroller from './components/Scroller.vue'

let install = Scroller.install = function (Vue, options = {}) {
  if (install.installed) return
  if (!options.scoped) {
    Vue.component(options.name || Scroller.name, Scroller)
  }
}

export default Scroller
