import CbStep from 'element-ui/packages/step/index.js'

const install = CbStep.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbStep', CbStep)
}

export default CbStep
