import CbMain from 'element-ui/packages/main/index.js'

const install = CbMain.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbMain', CbMain)
}

export default CbMain
