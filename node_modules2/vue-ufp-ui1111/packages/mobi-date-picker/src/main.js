import cbmDatePicker from './components/picker.vue'

let install = cbmDatePicker.install = function (Vue, options = {}) {
  if (install.installed) return
  if (!options.scoped) {
    Vue.component(options.name || cbmDatePicker.name, cbmDatePicker)
  }
}

export default cbmDatePicker
