import CbCarousel from 'element-ui/packages/carousel/index.js'

const install = CbCarousel.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbCarousel', CbCarousel)
}

export default CbCarousel
