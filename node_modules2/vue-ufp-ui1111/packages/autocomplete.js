import CbAutocomplete from 'element-ui/packages/autocomplete/index.js'

const install = CbAutocomplete.install = function(Vue, options = {}) {
  if (install.installed) return
  Vue.component('CbAutocomplete', CbAutocomplete)
}

export default CbAutocomplete
